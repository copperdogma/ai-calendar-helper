providers:
  - id: openai:gpt-4.1-nano
    config:
      temperature: 0
      response_format:
        type: json_object
      function_call:
        name: identify_events
      functions: file://functions/identify_events.yaml

defaultTest:
  assert:
    - type: javascript
      description: 'Valid JSON?'
      value: |
        try { JSON.parse(llmOutput); true; } catch { false; }
    - type: javascript
      value: file://scorers/event-count-accuracy.js

tests: file://fixtures/identification-dataset.jsonl

prompts:
  - file://prompts/identification.js
